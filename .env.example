# SimuTrace environment configuration
# No secrets or API keys required — all RPC calls are to public Soroban RPC endpoints.
# Optionally override the default testnet RPC URL:
# VITE_TESTNET_RPC=https://soroban-testnet.stellar.org
